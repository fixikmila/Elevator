public class TupleFloor {
    int current_floor, destination_floor, time_call;

    public TupleFloor(int current_floor, int destination_floor, int time_call) {
        this.current_floor = current_floor;
        this.destination_floor = destination_floor;
        this.time_call = time_call;
    }
}
